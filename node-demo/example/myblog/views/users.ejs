<%- include('header') %>
    <h1><%= name.toUpperCase()%></h1>
    <p>hello, <%= name %></p>
    <ul>
        <% for(var i = 0, len = testArray.length; i < len; i++) { %>
            <li><%= testArray[i] %></li>
        <% } %>
    </ul>
<%- include('footer') %>
